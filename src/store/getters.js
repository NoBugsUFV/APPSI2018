const getters = {
    GET_LEFT(state) {
        return state.leftsidebar
    },
    GET_RIGHT(state) {
        return state.rightsidebar
    },
    GET_TOKEN(state)
    {
        return state.token;
    }
}

export default getters